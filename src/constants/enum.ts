const UserStatus = {
  ACTIVE: 'active',
  INACTIVE: 'inactive',
};

const UserRole = {
  ADMIN: 'admin',
  USER: 'user',
}

const ActiveStatus = {
  ACTIVE: 'active',
  INACTIVE: 'inactive',
}

export { UserStatus, UserRole,  ActiveStatus };